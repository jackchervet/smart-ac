apiVersion: apps/v1
kind: Deployment
metadata:
  name: "smart-ac"
spec:
  selector:
    matchLabels:
      app: "smart-ac"
  template:
    metadata:
      labels:
        app: "smart-ac"
    spec:
      containers:
        - name: "smart-ac"
          image: "gcr.io/smart-ac/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "smart-ac"
spec:
  selector:
    app: "smart-ac"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080